ruby '2.3.0'

source 'https://rubygems.org'

# Padrino supports Ruby version 1.9 and later


# Distribute your app as a gem
# gemspec

# Server requirements
# gem 'thin' # or mongrel
# gem 'trinidad', :platform => 'jruby'

# Optional JSON codec (faster performance)
# gem 'oj'

# Project requirements
gem 'rake'

# Component requirements
gem 'bcrypt-ruby', :require => 'bcrypt' #, :platform => :ruby #gem install bcrypt --platform=ruby
gem 'haml'
gem 'mongoid'
gem 'sendgrid-ruby'
gem 'progress_bar'
# App requirements
gem 'mongoid-paranoia'
# gem 'mongoid-autoinc', :require => 'autoinc'
gem 'mongoid-slug'
gem 'mongoid-paperclip', :require => 'mongoid_paperclip'
gem 'aws-sdk', '~> 2', :require => 'aws-sdk'
gem 'pagarme'
gem 'ffi', :require => 'ffi' #gem install ffi --platform=ruby
gem 'mixpanel-ruby'
gem 'will_paginate', :require => 'will_paginate'
gem 'soundcloud'
gem 'business_time'
gem 'pagseguro-oficial', git: 'git://github.com/pagseguro/ruby.git', :require => 'pagseguro'
gem 'awesome_print'
gem 'money'
gem 'rmagick'
gem 'prawn'

gem 'progress_bar'
# Padrino Stable Gem
gem 'padrino', '0.13.2'

# Or Padrino Edge
# gem 'padrino', :github => 'padrino/padrino-framework'

gem 'rack-ssl-enforcer'
gem 'rack-no-www'

# Or Individual Gems
# %w(core support gen helpers cache mailer admin).each do |g|
#   gem 'padrino-' + g, '0.13.2'
# end
